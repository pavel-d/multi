language: generic

matrix:
  include:
   - name: amd64 bionic
     os: linux
     dist: bionic
     arch: amd64
   - os: arm64 xenial
     dist: xenial
     arch: arm64
   - os: arm64 bionic
     dist: bionic
     arch: arm64
   - os: ppc64le
     dist: xenial
     arch: ppc64le


script: uname -m
